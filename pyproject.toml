[project]
name = "geminizer-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "FJ-cyberzilla", email = "cyberzilla.systems@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "rich>=15.0.0",
    "typer>=0.27.0",
    "google-genai",
    "cryptography>=50.0.0",
]

[project.scripts]
geminizer-python = "geminizer_python:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
